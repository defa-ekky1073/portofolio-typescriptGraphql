// Define master schema for common type
// All condensed into one type so in the future it would easier to add new response
export const masterType = `
type MetaData {
    code: String
    message: String
}
`;